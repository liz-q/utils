const { describe, it } = require('mocha')
const { expect } = require("chai")
import virtualPagination from "../../../src/class/virtual-pagination";

describe("测试 virtualPagination 方法", function () {
  // it("", function () {
  //   expect().to.be.equal()
  // })
})
