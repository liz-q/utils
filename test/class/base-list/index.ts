const { describe, it } = require('mocha')
const { expect } = require("chai")
import baseList from "../../../src/class/base-list";

describe("测试 baseList 方法", function () {
  // it("", function () {
  //   expect().to.be.equal()
  // })
})
