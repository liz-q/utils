const { describe, it } = require('mocha')
const { expect } = require("chai")
import eventBus from "../../../src/class/event-bus";

describe("测试 eventBus 方法", function () {
  // it("", function () {
  //   expect().to.be.equal()
  // })
})
