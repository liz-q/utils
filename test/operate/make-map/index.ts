const { describe, it } = require('mocha')
const { expect } = require("chai")
import makeMap from "../../../src/operate/make-map";

describe("测试 makeMap 方法", function () {
  // it("", function () {
  //   expect().to.be.equal()
  // })
})
